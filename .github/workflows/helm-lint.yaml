name: Helm Lint and Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.14.0

      - name: Lint Helm Chart
        run: helm lint sonarqube-helm/

      - name: Template Helm Chart
        run: helm template test sonarqube-helm/

  validate:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.14.0

      - name: Validate with custom values
        run: |
          helm template test sonarqube-helm/ \
            --set domain=sonar.example.com \
            --set letsencrypt.email=test@example.com \
            --set postgres.password=testpassword
